{
  "install": "if ! command -v flutter &> /dev/null; then echo 'Installing Flutter...'; git clone https://github.com/flutter/flutter.git -b stable $HOME/flutter; fi && echo 'export PATH=\"$HOME/flutter/bin:$PATH\"' >> $HOME/.bashrc && export PATH=\"$HOME/flutter/bin:$PATH\" && flutter doctor && flutter pub get"
}
