{
  "install": "if ! command -v flutter &> /dev/null; then echo 'Installing Flutter...'; git clone https://github.com/flutter/flutter.git -b stable $HOME/flutter; fi && export PATH=\"$HOME/flutter/bin:$PATH\" && flutter doctor && flutter pub get",
  "start": "export PATH=\"$HOME/flutter/bin:$PATH\""
}
